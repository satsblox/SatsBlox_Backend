// Minimal Swagger/OpenAPI stub for local development.
// Keeping a small valid spec prevents swagger-ui from failing when mounted.

module.exports = {
	openapi: '3.0.0',
	info: {
		title: 'SatsBlox API (stub)',
		version: '0.0.1',
		description: 'Minimal stub of OpenAPI spec. Replace with full generated specs when available.',
	},
	paths: {},
};

